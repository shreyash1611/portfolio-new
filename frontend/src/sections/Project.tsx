export default function Project() {
  return (
    <section style={{ textAlign: "center", maxWidth: "36rem", margin: "0 auto" }}>
      <h1 style={{ marginTop: 0 }}>projects</h1>
      <p style={{ color: "var(--muted)", lineHeight: 1.6 }}>
        Project write-ups land here next — backend builds, data pipelines, and
        whatever else is worth showing. Placeholder for now so the nav stop
        already has its own page.
      </p>
    </section>
  );
}
